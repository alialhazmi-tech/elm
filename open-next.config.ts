import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// R2 incremental caching is intentionally deferred until the production
// Cloudflare account and bucket names are approved.
export default defineCloudflareConfig({});
