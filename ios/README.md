# تطبيق العلم — iOS

SwiftUI، iOS 17+. الرموز من `app/globals.css`. التوثيق: [`docs/ios/HANDOFF.md`](../docs/ios/HANDOFF.md).

```
open ios/Elm.xcodeproj
```

معلم M0: رموز + خطوط + تبويبات. الرئيسية الحقيقية في M1 عبر `GET /api/mobile/v1/home`.
